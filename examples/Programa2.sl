programa Programa2
{	
	{
		var x:int;
		var y:real;
		
		y = cast<real>(1);
		escrevaln "y=", y;
		
		escrevaln "Digite um número inteiro para x:";
		leia x;
		y = cast<real>(x);
		escrevaln "y=", y;
		
		var z:long;
		z = 3;
		escrevaln "z=", z;
	}
}